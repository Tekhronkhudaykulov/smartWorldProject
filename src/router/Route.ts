export enum APP_ROUTES {
  WELCOME = "/",
  AUTH = "/auth",

  FACE_ID = "/auth/face-id",
  TOUCH_ID = "/auth/touch-id",
  LOGIN = "/login",

  BANNER = "/banner",
  MAIN = "/main",
  MARKET = "/market/:id",
  FAVORITES = "/favorites",
  TRANSACTION = "/transaction",
  ACCESS = "/access",
}
